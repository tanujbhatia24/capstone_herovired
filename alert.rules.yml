groups:
- name: cost_alerts
  rules:
  - alert: HighCloudCost
    expr: some_metric > 500
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "Cloud cost exceeded threshold"
